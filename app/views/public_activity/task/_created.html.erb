<li class="<%= activity.id.in?(@unread_activities.collect{|a| a.id}) ? 'activity unread' : 'activity' %>" data-activity-id="<%= activity.uid %>">
	<% if activity.trackable %>
		<%= link_to read_activity_path(activity.uid) do %>
		  <%= render "public_activity/time", activity: activity %>
		  <div class="content">
	      <%= activity.owner.name %> created a task: <span class="highlight"><%= activity.trackable.content %></span>.
		  </div>
		 <% end %>
	<% else %>
		<%= render "public_activity/time", activity: activity %>
	  <div class="content">
	      <%= activity.owner.name %> created a task which has since been deleted.
	  </div>
	<% end %>
</li>