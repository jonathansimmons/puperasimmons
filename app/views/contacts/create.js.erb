$('#right_sidebar, body').removeClass('editing');
$('#form_container').html(" ");
<% if @group.singularize == @contact.contact_type %>
	$('ul.contacts').html("<%= j render Contact.where(contact_type: @contact[:contact_type]) %>")
<% end %>