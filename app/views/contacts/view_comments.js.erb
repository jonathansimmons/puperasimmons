$('#modals').html('<%= j render "comments/comments_modal", contact: @contact %>')
$('.modal').modal({
  keyboard: false,
  show: true,
  backdrop:true
})

	if ($("#comments ul").length > 0) {
		setTimeout(function(){
			$('#comments ul').scrollTop($('#comments ul').prop('scrollHeight'));
		},300)
	}